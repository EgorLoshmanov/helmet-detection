# FORTNITEBALLS — пайплайн проекта

Версия: 1.2  
Дата фиксации: 19 сентября 2026 года  
Последнее обновление: 22 сентября 2026 года  
Платформа: Firefly ROC-RK3588S-PC, Rockchip RK3588S  
Задача: обнаружение отсутствия защитной каски в реальном времени с физической сигнализацией

Этот файл является основным рабочим планом проекта. Выполненные пункты отмечаются флажком `[x]`. Существенные изменения модели, классов, оборудования или архитектуры записываются в раздел «Принятые решения».

## 1. Конечный результат

Система должна автоматически выполнять всю цепочку:

```text
Камера
  → получение кадра в реальном времени
  → preprocessing
  → RKNN-модель
  → инференс на NPU RK3588S
  → обнаружение helmet / no_helmet
  → временное подтверждение нарушения
  → включение LED и звукового сигнала
```

Демонстрация считается успешной, если человек без каски вызывает тревогу, надетая каска выключает тревогу, а отсутствие людей в кадре не вызывает ложный сигнал.

## 2. Принятые решения

- Команда: **FORTNITEBALLS**.
- Основная задача: object detection.
- Базовая модель для первого рабочего прототипа: **YOLOv8n**, вход `640 × 640`.
- Классы модели: `helmet` и `no_helmet`.
- Модель обучается или дообучается командой на собственном наборе данных.
- Базовый источник данных: открытый датасет касок и собственные кадры с используемой камеры.
- Финальный инференс выполняется в формате RKNN на NPU RK3588S.
- Firefly находится в университете и используется как удалённый стенд через NetBird и SSH.
- Обучение YOLO, экспорт ONNX и конвертация RKNN выполняются на рабочем компьютере. На Firefly передаются готовая RKNN-модель и приложение инференса.
- Исходный код хранится локально; развёртывание на плату выполняется воспроизводимым скриптом через SSH/SCP или rsync.
- Первая камера для прототипа: USB UVC. MIPI-CSI рассматривается после готовности основной цепочки.
- Периферия подключается поэтапно: консоль → встроенный LED → ESP32 по UART или внешний буззер.
- Прямое питание буззера от GPIO запрещено. Для прямого подключения используется транзистор или MOSFET.
- Если преподаватель потребует модель, отсутствующую в RKNN Model Zoo, YOLOv8n заменяется на YOLO26n. Остальная архитектура проекта сохраняется.
- Экспорт в ONNX/RKNN выполняется после дополнительного цикла сбора целевых кадров и дообучения: базовая модель хорошо работает на исходной валидации, но пока неудовлетворительно переносится на обычную веб-камеру.
- Датасет собирается из нескольких источников по конфигурации `dataset/sources.yaml`. Отображение классов задаётся отдельно для каждого источника, потому что одно и то же имя класса означает в разных наборах разное: `person` — это бокс человека в hard-hat-v1 и голова без каски в SHWD. Незаявленное имя класса прерывает сборку.
- Внешние источники, размечающие каски, но не головы без касок, не используются: неразмеченная голова обучается как фон и усиливает уже наблюдаемую ошибку.
- Второй стенд обучения — Windows с NVIDIA RTX 4060 Laptop, CUDA. Версии Python, PyTorch и Ultralytics совпадают со стендом macOS/MPS, поэтому результаты сравнимы.

## 3. Этап 1 — проверка аппаратной платформы

- [x] NetBird установлен и подключён к университетской сети.
- [x] Через `netbird status` определены состояние подключения и доступные peers.
- [x] Зафиксированы NetBird IP или hostname платы, SSH-пользователь и порт: `firefly@100.107.247.216:22`.
- [x] SSH-подключение выполняется по ключу без передачи пароля в файлах проекта; `BatchMode=yes` и `PasswordAuthentication=no` завершились с кодом `0`.
- [x] Проверена передача `PIPELINE.md` через SCP; локальная и удалённая SHA-256 совпали.
- [x] Firefly загружается в Ubuntu или Debian.
- [x] Настроено подключение по SSH.
- [x] Зафиксированы hostname платы и уникальный идентификатор стенда, чтобы не перепутать её с платами других команд: hostname `ubuntu`, NetBird IP `100.107.247.216`.
- [x] Зафиксированы версии ОС, ядра и драйвера RKNPU: Ubuntu 24.04.4, Linux 6.1.118, RKNPU 0.9.8.
- [x] Узлы видеотракта определяются в `/dev/video*`: MIPI/RKISP, основной поток `/dev/video11`, псевдоним `/dev/video-camera0`; физический CSI-сенсор не зарегистрирован.
- [ ] Получен тестовый кадр через V4L2, GStreamer или OpenCV — заблокировано отсутствием физического сенсора в media graph (`ENODEV`, `-19`).
- [ ] Проверен захват непрерывного видеопотока — заблокировано той же аппаратной проблемой камеры.
- [x] RKNN Runtime загружает тестовую модель: системная `mobilenet_v1.rknn` открыта через `rknn_common_test` от пользователя `firefly`.
- [x] Подтверждено выполнение тестового инференса на NPU: MobileNet, 3 прохода, `3.07/2.96/2.93 мс`, код возврата `0`.
- [x] Проверено программное управление встроенным LED: `:user` успешно переключён `0 → 1 → 0`.
- [x] При использовании ESP32 проверен UART7 `/dev/ttyS7`.

Фактически сохранено:

```text
docs/hardware.md
docs/ssh_known_hosts
```

Ещё предстоит оформить воспроизводимыми скриптами:

```text
tests/hardware/camera_test.*
tests/hardware/npu_test.*
tests/hardware/led_test.*
tests/hardware/uart_test.*
scripts/deploy.sh
```

Критерий завершения: камера, NPU и хотя бы один периферийный выход работают независимо друг от друга.

### Разделение локальной и удалённой работы

На рабочем компьютере выполняются:

```text
подготовка датасета
обучение YOLO
валидация PyTorch
экспорт и валидация ONNX
конвертация ONNX → RKNN
ведение исходного кода и документации
```

На Firefly выполняются:

```text
захват изображения с физической камеры
RKNN-инференс на NPU
postprocessing
логика SAFE / PENDING / ALARM
управление LED, UART, ESP32 или буззером
измерение производительности
```

Рекомендуемый цикл развёртывания:

```text
локальное изменение кода
  → автоматическая проверка
  → scripts/deploy.sh
  → копирование по SSH
  → запуск на Firefly
  → сохранение логов и метрик обратно в проект
```

## 4. Этап 2 — подготовка датасета

- [x] Выбран и скачан исходный открытый датасет: Safety Helmet Detection / Hard Hat Workers, 5000 изображений.
- [x] Проверены условия его использования и происхождение данных: Kaggle `andrewmvd/hard-hat-detection`, CC0 1.0 / Public Domain; источник и SHA-256 записаны в `dataset/README.md`.
- [x] Разметка приведена к классам `helmet` и `no_helmet`: `helmet → helmet`, `head → no_helmet`, `person` игнорируется.
- [x] Выполнена автоматическая проверка повреждённых изображений и некорректных bounding box: ошибок, повреждений, дубликатов и выходов за границы не обнаружено.
- [ ] Добавлены собственные кадры с целевой камеры — ожидается подключение камеры к стенду.
- [ ] Добавлены сложные примеры: кепки, капюшоны, частичные перекрытия, плохое освещение — часть присутствует в исходном наборе, но отдельная целевая подборка ещё не сформирована.
- [ ] Добавлены пустые кадры и сцены без людей — в исходном наборе отсутствуют.
- [x] Данные разделены на `train`, `val` и `test`: 4000 / 500 / 500 изображений, seed `20260919`.
- [ ] Кадры из одного видео не попадают одновременно в разные выборки — механизм готов (`group_by` в `dataset/sources.yaml` делит по группам, а не по кадрам), но исходный источник не предоставляет идентификаторы видео; правило вступит в силу на собственных записях.
- [x] Отобраны и проверены источники для версии v2: приняты SHEL5K и SHWD, отклонены IITU Safety-Helmet и Safety Helmet and Reflective Jacket. Причины и лицензии — в `dataset/README.md` и секции `rejected` файла `dataset/sources.yaml`.
- [x] Готова мультиисточниковая сборка: пер-источниковое отображение классов, поддержка Pascal VOC и YOLO, PNG и JPEG, перцептивная дедупликация между источниками и квотирование классов.
- [x] Написаны загрузчики внешних источников: `scripts/download_shwd.py` и `scripts/download_shel5k.py` с проверкой контрольных сумм, безопасной распаковкой и выводом реальных имён классов.
- [x] Версия v2 собрана: 5464 изображения, `helmet` 21 167, `no_helmet` 18 933, соотношение 1.12:1 против 3.28:1 в v1. Состав: SHEL5K 4902, SHWD 559, hard-hat-v1 3. Проверена загрузчиком Ultralytics, повреждённых файлов нет.
- [x] Дисбаланс классов устранён квотированием: дефицит `no_helmet` закрыт 559 изображениями SHWD, лимит 1200 не достигнут.
- [x] Обнаружены и удалены близкие дубликаты, которых прежний точный поиск не находил: 1880 изображений, из них 1727 внутри SHWD и 95 внутри SHEL5K.
- [x] Выполнена визуальная проверка случайной выборки разметки: 30 примеров в `reports/dataset_preview.html`.
- [x] Датасет полностью загружен и просканирован Ultralytics `8.4.155` под Python `3.10.21`: 5000 изображений, повреждённых файлов `0`.

Рекомендуемое разделение:

```text
train: 70–80%
val:   10–20%
test:  10%
```

Результаты этапа:

```text
dataset/processed/helmet_yolo_v1/images/{train,val,test}/
dataset/processed/helmet_yolo_v1/labels/{train,val,test}/
dataset/dataset.yaml
dataset/sources.yaml
dataset/README.md
scripts/prepare_dataset.py
scripts/download_shwd.py
scripts/download_shel5k.py
scripts/_datasets.py
reports/dataset_audit.json
reports/dataset_manifest.csv
reports/dataset_preview.html
reports/ultralytics_dataset_check.json
```

Текущая статистика объектов: `helmet` — 18 966, `no_helmet` — 5 785. Перед следующим циклом дообучения требуется дополнить набор собственными кадрами, пустыми сценами и целевой подборкой сложных отрицательных примеров.

План версии v2: SHEL5K заменяет неполную разметку на тех же 5000 изображениях, SHWD добирает класс `no_helmet` до соотношения около 1:1 подвыборкой, а не целиком, собственные кадры и пустые сцены закрывают целевой домен. Ни один внешний источник целевого домена не содержит, поэтому собственная съёмка обязательна. При первой сборке v2 перцептивная дедупликация впервые уберёт близкие кадры одной сцены, и число изображений станет меньше 5000.

Критерий завершения выполнен для базовой версии v1: датасет загружается Ultralytics без ошибок, а автоматическая и визуальная проверки разметки не выявили систематических проблем. Дополнение целевыми и сложными кадрами выполняется следующей версией датасета после подключения камеры.

## 5. Этап 3 — обучение YOLO

- [x] Зафиксированы Python `3.10.21`, Ultralytics `8.4.155`, PyTorch `2.14.0`, seed `20260919` и параметры обучения в `training/config.yaml`.
- [x] Выполнено базовое обучение YOLOv8n на Apple M3/MPS: конфигурация рассчитана на максимум 50 эпох с `patience=10`, в `results.csv` зафиксировано 47 завершённых эпох.
- [x] Сохранены лучшие и последние веса: `models/helmet_detector_best.pt` и `models/helmet_detector_last.pt`.
- [x] Зафиксированы метрики лучшей модели на исходной валидации: precision `0.936`, recall `0.874`, mAP50 `0.941`, mAP50–95 `0.618`.
- [x] Сохранены confusion matrix, PR/F1-кривые, графики обучения и примеры предсказаний в `reports/training/yolov8n_baseline/`.
- [ ] Модель формально проверена на независимой тестовой выборке — отдельный запуск на `test` с сохранением отчёта ещё не выполнен.
- [x] Выполнена предварительная проверка PyTorch-модели на встроенной камере Mac.
- [ ] Модель проверена на видео с целевой камеры Firefly — физическая камера к стенду ещё не подключена.
- [x] Выполнен предварительный анализ ложных срабатываний: на веб-камере Mac голова без каски, включая лысую голову, может ошибочно определяться как `helmet`.
- [x] Выполнен дополнительный цикл разметки и обучения: датасет v2 и модель `yolov8n_v2`.
- [x] Обучение на датасете v2 выполнено на Windows/CUDA: 50 эпох за 36.3 минуты (43.6 с на эпоху), precision `0.904`, recall `0.811`, mAP50 `0.885`, mAP50–95 `0.534` на валидации v2.
- [x] Дисбаланс предсказаний устранён: precision `helmet` `0.905` против `no_helmet` `0.903`, тогда как у baseline был крен в сторону `helmet`.
- [x] Обе модели сопоставлены на 190 изображениях, отложенных при обоих обучениях; выявлено, что соглашение о боксах класса `helmet` в SHEL5K отличается от v1, поэтому сравним только класс `no_helmet`: mAP50 `0.921` против `0.839`.
- [x] Зафиксировано время инференса на RTX 4060: 3.7 мс на изображение.

Результат базового эксперимента:

```text
На исходной валидации качество высокое, но перенос на веб-камеру неудовлетворительный.
Причины: сдвиг домена, дисбаланс helmet/no_helmet примерно 3.3:1,
нехватка собственных кадров, пустых сцен и сложных отрицательных примеров.
Порог confidence около 0.5 уменьшает слабые ложные детекции,
но не заменяет сбор целевых данных и дообучение.
```

Результаты этапа:

```text
training/train.py
training/config.yaml
models/helmet_detector_best.pt
models/helmet_detector_last.pt
reports/training/yolov8n_baseline/
```

Критерий завершения: PyTorch-модель стабильно различает `helmet` и `no_helmet` на независимом видео.

Текущий статус: критерий этапа не выполнен. До экспорта необходимо собрать целевой датасет v2, дообучить модель и повторить проверку на отдельном видео.

## 6. Этап 4 — экспорт и проверка ONNX

- [ ] `best.pt` экспортирован в ONNX.
- [ ] ONNX-файл проходит проверку структуры.
- [ ] Инференс ONNX Runtime работает на тестовых изображениях.
- [ ] Проверены preprocessing и postprocessing.
- [ ] Результаты PyTorch и ONNX сравнены на одинаковом наборе изображений.
- [ ] Координаты, классы и confidence отличаются только в допустимых пределах.

Контрольный набор должен содержать:

```text
helmet
no_helmet
несколько людей
пустой кадр
сложное освещение
частичное перекрытие головы
```

Результаты этапа:

```text
export/export_onnx.*
export/validate_onnx.*
models/helmet_detector.onnx
reports/onnx_comparison.json
```

Критерий завершения: ONNX выдаёт результаты, сопоставимые с исходной PyTorch-моделью.

## 7. Этап 5 — конвертация ONNX в RKNN

Конвертация выполняется на Linux x86 через RKNN-Toolkit2. На Firefly используется RKNN Runtime или RKNN-Toolkit-Lite2.

- [ ] Создан скрипт конвертации с `target_platform="rk3588"`.
- [ ] Получена FP16 RKNN-модель.
- [ ] FP16-модель проверена до начала квантизации.
- [ ] Подготовлен репрезентативный calibration dataset.
- [ ] Получена INT8 RKNN-модель.
- [ ] Сохранён лог конвертации.
- [ ] Проверены предупреждения о неподдерживаемых операциях.
- [ ] Сравнены результаты ONNX, RKNN FP16 и RKNN INT8.

Результаты этапа:

```text
export/convert_rknn.py
export/calibration_dataset.txt
models/helmet_detector_fp16.rknn
models/helmet_detector_int8.rknn
reports/rknn_comparison.json
```

Критерий завершения: обе RKNN-модели запускаются, а INT8 не имеет критического падения качества.

## 8. Этап 6 — инференс RKNN на Firefly

- [ ] RKNN-модель загружается на Firefly.
- [ ] Инференс выполняется через NPU.
- [ ] Проверены формат входа, порядок каналов и нормализация.
- [ ] Реализован letterbox с корректным восстановлением координат.
- [ ] Реализованы декодирование выходов и NMS.
- [ ] Результат сохраняется как изображение с bounding box.
- [ ] RKNN проверен на том же контрольном наборе, что PyTorch и ONNX.
- [ ] Зафиксированы версии Toolkit, Runtime и драйвера.

Особое внимание:

```text
RGB / BGR
NHWC / NCHW
INT8 input scale и zero point
padding после letterbox
масштабирование bounding box
confidence threshold
NMS threshold
```

Критерий завершения: статические изображения корректно обрабатываются непосредственно на Firefly.

## 9. Этап 7 — обработка видео в реальном времени

- [ ] Реализован непрерывный захват камеры.
- [ ] Захват изображения отделён от инференса.
- [ ] Обрабатывается последний доступный кадр без накопления очереди.
- [ ] Выполняется RKNN-инференс.
- [ ] На изображении отображаются класс, confidence, bounding box и FPS.
- [ ] Корректно обрабатывается потеря камеры.
- [ ] Приложение корректно освобождает камеру и RKNN Runtime.

Архитектура приложения:

```text
Camera capture
  → latest frame buffer
  → preprocessing
  → RKNN inference
  → postprocessing
  → visualization
  → safety state machine
  → alarm controller
```

Критерий завершения: видеопоток работает стабильно и без постоянного роста задержки.

## 10. Этап 8 — логика безопасности

Состояния системы:

```text
SAFE     — подтверждённых нарушений нет
PENDING  — no_helmet появился, ожидается подтверждение
ALARM    — нарушение подтверждено
```

- [ ] Пустой кадр без людей считается безопасным.
- [ ] Одиночная ошибочная детекция не включает тревогу.
- [ ] Тревога включается после нескольких подтверждений `no_helmet`.
- [ ] Тревога выключается только после серии безопасных результатов.
- [ ] Порог confidence вынесен в конфигурацию.
- [ ] Задержки включения и выключения вынесены в конфигурацию.
- [ ] Логика проверена для одного и нескольких людей.

Начальные параметры для настройки:

```text
alarm_on_frames:  5–10
alarm_off_frames: 15–30
no_helmet_conf:   около 0.5
```

Критерий завершения: система не дребезжит между состояниями и не включает тревогу в пустом помещении.

## 11. Этап 9 — интеграция периферии

- [ ] Состояние тревоги выводится в консоль.
- [ ] Состояние тревоги управляет встроенным LED.
- [ ] Выбран окончательный вариант подключения звука.
- [ ] Для ESP32 реализован UART-протокол.
- [ ] Для прямого подключения буззера собрана безопасная транзисторная схема.
- [ ] При запуске приложения периферия переходит в безопасное состояние.
- [ ] При аварийном завершении сигнал выключается.
- [ ] Добавлено ограничение продолжительности непрерывного звука.

Минимальный UART-протокол:

```text
ALARM:1\n
ALARM:0\n
```

Критерий завершения: результат нейронной сети автоматически вызывает физическую реакцию без ручного вмешательства.

## 12. Этап 10 — тестирование и измерения

- [ ] Измерено чистое время одного RKNN-инференса.
- [ ] Измерен end-to-end FPS.
- [ ] Измерена задержка от появления нарушения до включения сигнала.
- [ ] Зафиксирован размер PT, ONNX и RKNN.
- [ ] Сравнено качество FP16 и INT8.
- [ ] Проверена работа при одном и нескольких людях.
- [ ] Проверены разные расстояния и освещение.
- [ ] Подсчитаны ложные тревоги и пропущенные нарушения.
- [ ] Проведён продолжительный тест стабильности.

Итоговая таблица:

| Модель | Формат | Precision | Recall | mAP50–95 | Инференс, мс | FPS | Размер |
|---|---|---:|---:|---:|---:|---:|---:|
| YOLOv8n | PyTorch | 0.936 | 0.874 | 0.618 | — | — | 6.0 МБ |
| YOLO | ONNX |  |  |  |  |  |  |
| YOLO | RKNN FP16 |  |  |  |  |  |  |
| YOLO | RKNN INT8 |  |  |  |  |  |  |

Критерий завершения: результаты воспроизводимы и записаны в `benchmarks/` и `reports/`.

## 13. Этап 11 — упаковка проекта

Целевая структура:

```text
FORTNITEBALLS/
├── README.md
├── PIPELINE.md
├── environment.yml
├── requirements-win-cuda.txt
├── configs/
├── dataset/
│   ├── sources.yaml
│   └── README.md
├── scripts/
├── training/
├── export/
├── models/
├── firefly_app/
│   ├── camera.py
│   ├── inference.py
│   ├── postprocess.py
│   ├── safety_state.py
│   └── peripheral/
├── esp32/
├── tests/
├── benchmarks/
├── reports/
└── docs/
```

- [x] Создан `README.md` с установкой, подготовкой данных и локальным запуском.
- [x] Зафиксированы зависимости и их версии в `environment.yml`; CUDA-сборка PyTorch для Windows вынесена в `requirements-win-cuda.txt`, поскольку обычный PyPI отдаёт там CPU-сборку и обучение молча уходит на процессор.
- [x] Добавлены команды подготовки данных, проверки, обучения и PyTorch-инференса.
- [x] Baseline-веса опубликованы как GitHub pre-release `v0.1.0`; загрузка и SHA-256 проверены из чистого клона.
- [ ] Веса модели v2 опубликованы как релиз `v0.2.0` — `scripts/download_model.py` уже знает обе версии и контрольные суммы, сам релиз ещё не создан.
- [ ] Добавлены команды экспорта ONNX и конвертации RKNN.
- [ ] Добавлена команда запуска приложения на Firefly.
- [ ] Приложена схема подключения периферии.
- [ ] Приложены метрики и результаты тестирования.
- [ ] Добавлено краткое описание архитектуры.
- [ ] Проект проверен по инструкции на чистом окружении.

## 14. Этап 12 — финальная демонстрация

Сценарий защиты:

1. Запустить приложение одной командой.
2. Показать видеопоток с камеры.
3. Показать, что используется RKNN-модель и NPU.
4. Войти в кадр в каске — состояние `SAFE`.
5. Снять каску — после подтверждения состояние `ALARM`.
6. Показать включение LED и звукового сигнала.
7. Надеть каску — тревога автоматически выключается.
8. Показать FPS, время инференса и сравнение моделей.

## 15. Общие критерии готовности

Проект готов, если одновременно выполнено следующее:

- [x] Базовая модель обучена командой.
- [x] Сохранены датасет, конфигурация и результаты базового обучения.
- [x] PyTorch-модель технически загружается и выполняет инференс; качество на целевых сценах ещё не принято.
- [ ] ONNX-модель работает и проверена.
- [ ] RKNN-модель работает на NPU RK3588S.
- [ ] Используется поток с реальной камеры.
- [ ] Реализована устойчивая логика `SAFE/PENDING/ALARM`.
- [ ] Результат модели управляет реальной периферией.
- [ ] Измерены скорость, задержка, качество и размер модели.
- [ ] Проект запускается по README без скрытых ручных настроек.

## 16. Журнал изменений решений

| Дата | Решение | Причина |
|---|---|---|
| 19.09.2026 | Зафиксирован базовый пайплайн | Начало разработки проекта FORTNITEBALLS |
| 19.09.2026 | Базовая модель — YOLOv8n | Минимальный риск при переносе в RKNN и наличие официального примера |
| 19.09.2026 | Классы — helmet / no_helmet | Отсутствие helmet само по себе не доказывает нарушение |
| 19.09.2026 | Сначала LED, затем звуковая периферия | Поэтапная проверка интеграции и упрощение диагностики |
| 19.09.2026 | Firefly используется удалённо через NetBird и SSH | Платы физически находятся в университете |
| 19.09.2026 | Стенд команды — `100.107.247.216` | Пользователь подтвердил NetBird IP платы FORTNITEBALLS |
| 19.09.2026 | Базовый датасет — Safety Helmet Detection / Hard Hat Workers | 5000 изображений, разметка Pascal VOC, понятная лицензия CC0 и подходящие классы `helmet`/`head` |
| 20.09.2026 | Завершено базовое обучение YOLOv8n | Получены `best.pt`, `last.pt`, метрики и графики; в журнале обучения 47 эпох |
| 20.09.2026 | Базовая модель проверена на камере Mac | Выявлены ложные определения головы без каски как `helmet` |
| 22.09.2026 | Экспорт ONNX/RKNN отложен до датасета v2 | Требуются собственные кадры, баланс классов, пустые сцены и сложные отрицательные примеры |
| 22.09.2026 | Добавлен воспроизводимый старт для команды | README, загрузчики датасета/модели и автоматический выбор CUDA/MPS/CPU позволяют продолжить работу после клона |
| 22.09.2026 | Опубликован GitHub pre-release `v0.1.0` | `helmet_detector_best.pt` доступен через проверенный загрузчик; SHA-256 совпал при проверке из чистого клона |
| 22.09.2026 | Сборка датасета переведена на мультиисточниковую схему | Отображение классов должно задаваться отдельно для каждого источника: `person` означает бокс человека в hard-hat-v1 и голову без каски в SHWD |
| 22.09.2026 | Незаявленное имя класса прерывает сборку | Прежняя схема молча пропускала неизвестные имена, что превращало размеченные объекты в фон без единого предупреждения |
| 22.09.2026 | Приняты SHEL5K и SHWD | SHEL5K восполняет неполную разметку тех же 5000 изображений; SHWD — единственный источник с плотно размеченной головой без каски |
| 22.09.2026 | Отклонены IITU Safety-Helmet и Safety Helmet and Reflective Jacket | В обоих размечены только каски: непокрытые головы остаются фоном и усиливают имеющуюся ошибку |
| 22.09.2026 | SHWD используется подвыборкой, а не целиком | Соотношение 9 044 к 111 514 перевернуло бы баланс классов; кроме того его классы лежат в непересекающихся пулах кадров и поощряют обучение по контексту сцены |
| 22.09.2026 | Добавлена перцептивная дедупликация между источниками | Одно и то же изображение в другом формате даёт другой SHA-256, и при смешивании источников утекало бы между `train` и `val` |
| 22.09.2026 | Добавлен второй стенд обучения на Windows и CUDA | RTX 4060 Laptop при совпадающих версиях Python, PyTorch и Ultralytics; `environment.yml` без явного пина torch давал на Windows CPU-сборку |
| 22.09.2026 | Подтверждено, что SHEL5K содержит те же файлы, что hard-hat-v1 | Имена совпадают, SHA-256 выборки из 200 PNG совпал побайтово; замена разметки не требует новых изображений |
| 22.09.2026 | Отображение `head_with_helmet` оставлено в `null` | Его бокс почти совпадает с боксом каски, и оба класса дали бы по два перекрывающихся объекта на голову; альтернативная постановка «голова в каске как класс 0» описана в `dataset/README.md` и пока не принята |
| 22.09.2026 | Целостность JPEG проверяется декодированием, а не наличием EOI | Требование маркера EOI отбрасывало 58 пригодных изображений SHWD: у JPEG нет контрольных сумм, а реальные архивы содержат файлы с паддингом и дописанным хвостом |
| 22.09.2026 | Собрана версия v2 | Соотношение классов 1.12:1 против 3.28:1; для сравнения с baseline пригодна только проверка на отдельном целевом видео, так как состав кадров изменился |
| 22.09.2026 | Изображения с нестандартным хвостом JPEG копируются, а не линкуются | Ultralytics перезаписывает такие файлы на месте, и сквозь жёсткую ссылку это портило исходный архив: при первом прогоне так было переписано 9 файлов SHWD |
| 22.09.2026 | Обучение переведено на `dataset/dataset_v2.yaml`, прогон назван `yolov8n_v2` | Baseline Егора в `reports/training/yolov8n_baseline/` сохраняется без изменений |
| 22.09.2026 | Обнаружено взаимное загрязнение сплитов v1 и v2 | 404 из 500 тестовых изображений v1 лежат в обучающей выборке v2; прямое сравнение моделей на этих сплитах недостоверно, корректная почва — 190 отложенных обоими обучениями изображений |
| 22.09.2026 | Обнаружено расхождение соглашения о боксах класса `helmet` | В SHEL5K `helmet` — бокс самой каски (0.52 площади, совпадение 46%), геометрию v1 несёт `head_with_helmet` (0.87 площади, совпадение 75%); решение о смене отображения отложено |
| 22.09.2026 | `scripts/download_model.py` поддерживает несколько релизов | Сравнение новой модели с предыдущей требует прежних весов, а в Git они не хранятся; по умолчанию загружается `v0.2.0` |
